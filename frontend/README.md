# Frontend (React + Vite)

Démarrage rapide :

```bash
cd frontend
npm install
npm run dev
```

L'application appelle l'API à `/api/*` — si le backend Symfony tourne sur le même domaine et port, les sessions cookies seront partagés.
